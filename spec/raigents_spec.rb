# frozen_string_literal: true

RSpec.describe Raigents do
  it "has a version number" do
    expect(Raigents::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
